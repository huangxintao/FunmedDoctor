<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.funmed.funmeddoctor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".home.activity.MainTabActivity"/>
        <activity android:name=".mine.activity.LoginActivity"/>
        <activity android:name=".clinical.activity.DetectionSchemeActivity"/>
        <activity android:name=".mine.activity.RegisterActivity"/>
        <activity android:name=".clinical.activity.SBKDetectionActivity"/>
        <activity android:name=".scientific.activity.ScientificProjectOrderPlaceActivity"/>
        <activity android:name=".scientific.activity.NormalDetectionActivity"/>
    </application>

    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>